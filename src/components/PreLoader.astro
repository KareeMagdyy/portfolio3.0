<div class='d-flex align-items-center justify-content-center' id='pre-loader'>
  <div class='img-wrapper'>
    <img src='/assets/images/logo-light.png' alt='Logo' loading='eager' />
    <h6 class='text-center mt-1'>Loading...</h6>
  </div>
</div>

<style>
  body {
    overflow-y: hidden;
  }
  #pre-loader {
    position: fixed;
    inset: 0;
    overflow-y: hidden;
    z-index: 99999;
    background-color: black;
  }

  .img-wrapper {
    width: 250px;
    position: relative;
  }

  .img-wrapper:after {
    position: absolute;
    content: "";
    inset: 0;
    height: 110%;
    background-color: black;
    transform: translateX(100%);
    animation: loading 4s ease-in alternate infinite;
    transition: all 350ms linear;
  }

  .img-wrapper img {
    max-width: 100%;
  }
  .img-wrapper h6 {
    opacity: 0;
    animation: opacity 1s ease-in alternate infinite;
  }

  @keyframes loading {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(125%);
    }
  }

  @keyframes opacity {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }
</style>
